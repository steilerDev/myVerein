<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="  http://www.springframework.org/schema/beans
                                   http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                                   http://www.springframework.org/schema/security
                                   http://www.springframework.org/schema/security/spring-security.xsd">

       <!-- Defining pages that are secured through Spring security -->
       <http auto-config="false"  use-expressions="true" access-denied-page="/accessdenied">
            <intercept-url pattern="/login" access="permitAll" />
            <intercept-url pattern="/logout" access="permitAll" />
            <intercept-url pattern="/signup" access="permitAll" />
            <intercept-url pattern="/accessdenied" access="permitAll" />
            <intercept-url pattern="/settings" access="hasRole('ROLE_SUPERADMIN')" />
            <intercept-url pattern="/**" access="hasRole('ROLE_ADMIN')" />

            <form-login login-page="/login"/>
            <logout logout-url="/logout" />
       </http>


       <authentication-manager>
              <!--  Plain text authentication without database -->
              <!-- authentication-provider>
                     <user-service properties="classpath:admin.properties" />
              </authentication-provider-->
              <authentication-provider user-service-ref="UserAuthenticationService">
                     <password-encoder ref="passwordEncoder">
                            <salt-source user-property="salt" />
                     </password-encoder>
              </authentication-provider>
       </authentication-manager>

        <!-- Password encoder with SHA-512 hashing method and 1000 iterations using 8byte salting -->
       <beans:bean id="passwordEncoder" class="de.steilerdev.myVerein.server.security.PasswordEncoder" />

       <beans:bean id="UserAuthenticationService" class="de.steilerdev.myVerein.server.security.UserAuthenticationService" />

</beans:beans>